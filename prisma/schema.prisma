generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:Ph101912848$@db.bnjcklhtmxxsdvaqzmpj.supabase.co:5432/postgres"
}

model Person {
  id            Int     @id @default(autoincrement())
  name          String  @db.VarChar
  mother_id     Int     @default(0)
  father_id     Int     @default(0)
  surname       String
  birth_surname String?
  year_of_birth Int?
  year_of_death String?
  birth_place   String?
}

model user {
  id       Int    @id @default(autoincrement())
  username String
  password String
}
